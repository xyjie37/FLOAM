最推荐的定义
设 (f_\tau(x)\in\mathbb R^d) 表示任务 (\tau) 时的特征提取器输出。
对任一样本 (x_i)，先定义归一化特征：
[
\tilde z_i^{(\tau)}=\frac{f_\tau(x_i)}{|f_\tau(x_i)|_2}.
]
对于客户端 (k)、任务 (\tau)、类别 (y)，记该客户端在该任务上属于类别 (y) 的样本集合为
[
\mathcal D_{k,\tau}^{(y)}={x_i \mid x_i \text{ belongs to class } y},
\qquad
n_{k,\tau}{(y)}=|\mathcal D_{k,\tau}{(y)}|.
]
定义该类的归一化原型为
[
p_{k,\tau}^{(y)}
=
\frac{
\frac{1}{n_{k,\tau}{(y)}}\sum_{x_i\in \mathcal D_{k,\tau}{(y)}} \tilde z_i^{(\tau)}
}{
\left|
\frac{1}{n_{k,\tau}{(y)}}\sum_{x_i\in \mathcal D_{k,\tau}{(y)}} \tilde z_i^{(\tau)}
\right|_2
}.
]
下面两个指标都基于这个 (p_{k,\tau}^{(y)})。

1. Spatial Misalignment Index (SMI)
SMI 用来度量：同一任务下，同一类别在不同客户端之间的表征是否一致。
令 (\mathcal K_{\tau}^{(y)}) 表示在任务 (\tau) 中拥有有效类别 (y) 原型的客户端集合。
则定义
[
\mathrm{SMI}\tau
=
\frac{1}{|\mathcal Y\tau|}
\sum_{y\in\mathcal Y_\tau}
\frac{2}{|\mathcal K_{\tau}{(y)}|\bigl(|\mathcal K_{\tau}{(y)}|-1\bigr)}
\sum_{\substack{k,k'\in\mathcal K_{\tau}^{(y)}\ k<k'}}
\left(1-p_{k,\tau}{(y)\top}p_{k',\tau}{(y)}\right).
]
解释
● (1-p^\top q) 是 cosine dissimilarity；
● SMI 越小，说明不同客户端对同一类的语义编码越一致；
● SMI 越大，说明跨客户端类表征越分裂。

2. Temporal Drift Index (TDI)
TDI 用来度量：同一客户端内，同一类别从任务 (\tau-1) 到任务 (\tau) 的表征漂移有多大。
令 (\mathcal V_\tau) 表示所有在任务 (\tau-1) 和 (\tau) 中都具有有效类别原型的客户端-类别对：
[
\mathcal V_\tau
=
{(k,y)\mid y \text{ has valid prototypes on client } k \text{ at both } \tau-1 \text{ and } \tau}.
]
则定义
[
\mathrm{TDI}\tau
=
\frac{1}{|\mathcal V\tau|}
\sum_{(k,y)\in\mathcal V_\tau}
\left(1-p_{k,\tau-1}{(y)\top}p_{k,\tau}{(y)}\right).
]
解释
● TDI 越小，说明同一客户端对同一类别的表示随任务演化更稳定；
● TDI 越大，说明持续学习过程中该类别语义方向漂移更明显。